#!/bin/bash
if [ $# -eq 0 ]; then
  echo "Usage: $0 <script_name.sh> [args...]"
  exit 1
fi
script_name="$1"
shift
if [ ! -f "scripts/$script_name" ]; then
  echo "Script scripts/$script_name not found"
  exit 1
fi
bash "scripts/$script_name" "$@"